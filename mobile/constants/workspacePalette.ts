// Matches extension's WS_COLORS exactly (SettingsState.tsx) — a fixed
// swatch set, separate from PROJECT_PALETTE (extension keeps its own
// distinct palette for workspaces too).
export const WORKSPACE_PALETTE = ['#C8553D', '#4A6FA5', '#2D8A7A', '#7B5DB4', '#B07A1F', '#2A7A4A'] as const;
