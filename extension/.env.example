# Copy to .env.development and .env.production and fill in the values.
# Obtain credentials from Google Cloud Console → APIs & Services → Credentials.
# OAuth 2.0 client type: Desktop app (safe to embed in extensions per Google policy).
# Add the extension redirect URI to authorized redirect URIs:
#   https://kloaflkoffpkpldhaipegeabhhkijhbj.chromiumapp.org
VITE_GOOGLE_CLIENT_ID=
VITE_GOOGLE_CLIENT_SECRET=
